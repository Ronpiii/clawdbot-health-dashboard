# 2026-02-11

## nightly build: arc clean (00:00 UTC)

### what
- built `arc clean` — workspace hygiene scanner and cleaner
- scans for: large files (>5MB), temp/junk files, empty dirs, .next caches, stale git branches, node_modules bloat, git gc opportunities
- full report: size breakdown, per-category listings, hygiene score (0-100)
- `--fix` mode: safely cleans temp files, empty dirs, .next caches, runs git gc — never touches node_modules or branches
- `--short` mode: one-liner summary
- `--json` mode: machine-readable output
- aliases: `arc clean`, `arc tidy`, `arc sweep`
- added routing in arc CLI

### first run results
- workspace: 1.50G → 1.35G (155MB reclaimed)
- cleared 4 .next caches (139MB), 2 empty dirs, git gc on 6 repos (16.3MB)
- found 2 merged branches in anivia (feature/campaign-hub, feature/prospects-view)
- hygiene score: 78/100 → 90/100 after fix

### why
- ron has 1.5GB workspace with 5+ repos, .next caches accumulating, 14k loose git objects
- no tool existed to see or clean this — `arc health` checks project status, not disk hygiene
- first-run-useful: actually cleaned 155MB on first `--fix`
