# 2026-03-03

## morning standup (06:00 UTC)
- morning.mjs posted to discord (weather timed out again — wttr.in chronic)
- overnight: built `arc ship` — nightly build #33
- yesterday: collabo-v2 rebuild spec + debugging, ron building with conductor
- no urgent carryover

## nightly build #33: arc ship (00:00 UTC)

### what
- built `arc ship` — pre-deploy flight checklist
- 5 checks: git state, code review (diff-based), service health, env audit, lockfiles
- weighted composite score → SHIP/HOLD/ABORT verdict
- per-project mode: `arc ship anivia` → 98/100 SHIP
- full workspace: 86/100 HOLD (4 dirty repos, 1 unpushed commit)
- auto-fix mode pushes unpushed commits
- CI-friendly exit codes (0/1/2)

### design decisions
- lightweight versions of existing checks (review, pulse, env, shield) — not slow full scans
- env severity tuned: deployed-only projects without local .env = low severity, not high
- stale branches at info level (14d threshold) — informational, doesn't kill score
- services use HEAD requests with 8s timeout, parallel
- verdict thresholds: SHIP ≥75, HOLD 50-75, ABORT <50 or any criticals
- git weight highest (30%) — dirty state is the most common deploy mistake

### first run
- full workspace: 86/100 HOLD — git 67, review 100, services 100, env 85, lockfiles 84
- anivia-specific: 98/100 SHIP — nearly clean
- flagged: 4 auto-fixable issues (unpushed commits)

committed and pushed: `099276d`

## morning heartbeat (06:00 UTC)
- anivia: 6 sequences checked, 0 advanced
- moltbook: no DMs, queue empty
- git: dirty tuner submodule + today's log (normal)
- reddit pulse: blocked — brave API key still missing (day 3+)
- curiosity drop: blocked — same reason
- **action needed**: ron should run `clawdbot configure --section web` to set BRAVE_API_KEY. unblocks reddit scanning, curiosity drops, and web search in general

## collabo-v2 workspace bug (10:30 UTC)
- ron hit "No workspace" error when creating a client in collabo-v2
- **root cause**: signing in via `/login` skips workspace creation. onboarding calls `supabase.rpc("create_workspace_for_user")` but login doesn't check for existing workspace
- **fix needed**: dashboard layout should check if user has a workspace, redirect to onboarding step 2 if not
- ron fixing with conductor — doesn't need my help on the code
- cloned repo to /tmp/collabo-v2 using a PAT token (now deleted/rotated)
- **repo access**: Ronpiii/collabo-v2 is private, my gh token can't see it. ron shared a PAT but it's been rotated

## collabo agentic architecture discussion (11:53 UTC)
- ron asked about adding agentic AI to collabo
- recommended 3-tier approach:
  - **tier 1 (MVP)**: upgrade Cmd+K to natural language → API route with function calling → existing CRUD
  - **tier 2**: proactive background jobs (stale task detection, weekly digest, smart triage)
  - **tier 3**: autonomous (task decomposition, meeting→tasks, project health scoring)
- key architecture: one set of tools, both reactive and proactive agents use same function definitions
- don't build a chat sidebar — upgrade Cmd+K instead

## youtube transcription (12:49 UTC)
- transcribed "5 Keys to Design AWWWARD Winning Websites" by Adrian Samosa
- installed yt-dlp on server for future use
- saved clean transcript + summary to `memory/transcripts/awwwards-5-keys.md`
- 5 keys: few ingredients, master typography, master composition, master animation, master art direction

## ventok.eu website audit verification (13:42-14:50 UTC)
- ron shared a full audit of ventok.eu (from conductor or another tool)
- i verified every claim against the live site

### confirmed real issues:
1. **mobile nav hidden** — CSS `display:none!important` on ≤1023px, BUT hamburger menu now exists (was fixed since audit)
2. **old routes 404** — `/services/workflow-automation`, `/book-a-call` → 404
3. **email inconsistency** — `hello@ventok.ee` (main site) vs `ronald@ventok.eu` (privacy policy)
4. **phone hidden** — `+372 555 13 976` only in terms page
5. **og:locale** — set to `en_US`, should be `en_EE`

### false/already fixed (5 of 12 claims were wrong):
- language switcher EXISTS (EN/ET toggle in nav)
- sitemap URLs ALL work (200, not 404)
- custom 404 page EXISTS (branded)
- logo has alt text
- "let's talk" opens contact panel (not broken)

### ventok.eu site location:
- **live files**: `/data02/virt137413/domeenid/www.ventok.eu/htdocs/`
- static HTML export (Next.js SSG), NOT the payload CMS project at `projects/ventok-website`
- has `responsive-fixes.css`, `mobile-nav.js`, `contact-handler.js`, `seo-a11y-patch.js`

### mobile hero whitespace fix:
- ron reported huge gap between hero CTAs and stats on mobile
- cause: `responsive-fixes.css` had `min-height: calc(100svh - 65px)` + `justify-content: center` on hero
- ron wants hero to fill viewport with CTAs at bottom, space above title
- **applied fix**: changed `justify-content: center` → `flex-end`, reduced `padding-bottom: 6rem` → `2.5rem`
- ron may want further tweaks — waiting on feedback

## ventok visual direction (14:28 UTC)
- ron wants to use excalidraw-style visuals for ventok.eu
- recommended hybrid approach: excalidraw for services/workflow diagrams (on-brand), clean design everywhere else
- don't go 100% excalidraw — would look template-y
- key insight from awwwards video: do what's right for the brand, not what's trendy

## clawdbot update attempt (16:36 UTC)
- ron asked to update clawdbot (referenced openclawd tweet)
- `npm update -g clawdbot` ran — changed 2 packages but version stayed at `2026.1.24-3`
- no new clawdbot release on npm — tweet may have been about something else
- couldn't read the tweet (x.com blocks scraping, no brave API key)

## blockers (ongoing)
- **brave API key**: day 3+ missing. blocks web search, reddit pulse, curiosity drops
- **ventok source code**: live site is static HTML at zone htdocs, no git repo for it. edits are direct file changes
- **collabo-v2 repo**: private, no access via my gh token
